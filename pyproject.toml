[project]
name = "treeq"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "VINODvoid", email = "kalki.the.dev@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
treeq = "treeq:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
